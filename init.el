;; Rubicon's Emacs Configuration

(add-to-list 'load-path (concat user-emacs-directory "el"))

(require 'base)

(require 'theme)

(require 'completion)

(require 'workspace)

(require 'splash)

(require 'vcs)

;; Lanuage supports
(require 'lang-lisp)
(require 'lang-python)
